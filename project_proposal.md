# 터미널 기반 객체지향 C++ 체스 게임 개발 및 엔진 확장성 설계

## 서론

본 보고서는 터미널 환경에서 실행 가능한 2인용 체스 게임을 C++로 개발하는 과정을 상세히 다룹니다. 이 프로젝트의 핵심 목표는 단순히 게임을 구현하는 것을 넘어, 객체지향 설계 원칙에 따라 보드(Board), 기물(Piece), 수(Move)를 명확한 객체로 모델링하는 것입니다. 더 나아가, 향후 독자적인 체스 엔진(AI)을 탑재할 수 있도록 시스템의 확장성과 성능을 고려한 아키텍처 설계를 제시합니다.

보고서는 네 부분으로 구성됩니다. 
1.  **기초 아키텍처**: 객체지향의 핵심인 `Piece`, `Board`, `Move` 클래스 설계를 통해 게임의 견고한 기반을 다집니다.
2.  **게임 로직**: 캐슬링, 앙파상 등 체스의 복잡한 규칙을 C++ 코드로 구현하는 방법을 논합니다.
3.  **사용자 인터페이스**: 터미널 인터페이스를 구축하고 표준 대수 표기법(Standard Algebraic Notation, SAN)을 파싱하여 사용자 입력을 처리하는 기법을 다룹니다.
4.  **엔진 확장성**: 단순한 2인용 게임의 한계를 넘어 고성능 체스 엔진으로 확장하기 위한 로드맵을 제시하며, 비트보드(Bitboard)와 같은 고급 데이터 구조와 미니맥스(Minimax) 탐색 알고리즘의 기초를 설명합니다.

이 보고서는 C++에 대한 기본적인 이해를 갖춘 개발자가 소프트웨어 아키텍처, 객체지향 설계, 그리고 체스 프로그래밍의 특수한 문제들을 깊이 있게 이해할 수 있도록 돕는 것을 목표로 합니다.

---

## 제 1부: 기초 아키텍처 - 객체지향의 핵심

체스 게임의 복잡성을 효과적으로 관리하기 위한 첫걸음은 잘 설계된 객체지향 아키텍처를 구축하는 것입니다. 이 부분에서는 게임의 핵심 구성 요소인 기물, 보드, 수를 각각의 클래스로 모델링하여 명확한 책임 분리, 현대적인 C++ 관용구 사용, 그리고 직관적이면서도 강력한 시스템 모델을 만드는 데 집중합니다.

### 1.1 기물 계층 구조: 다형성의 활용

체스의 다양한 기물들은 공통된 속성(색상, 유형)을 공유하면서도 각기 다른 이동 규칙을 가집니다. 이러한 "is-a" 관계를 표현하기에 가장 적합한 객체지향 패턴은 상속을 통한 다형성 구현입니다.

#### 추상 기본 클래스 `Piece`
모든 체스 기물의 공통 속성과 행위를 정의하는 추상 기본 클래스 `Piece`를 설계합니다.

-   **공통 속성**:
    -   `Color`: 기물의 색상을 나타내는 `enum class Color { WHITE, BLACK };`
    -   `PieceType`: 기물의 종류를 나타내는 `enum class Type { PAWN, KNIGHT, BISHOP, ROOK, QUEEN, KING };`
    -   `Square`: 기물의 현재 위치를 나타내는 참조.
-   **순수 가상 함수**:
    -   `virtual std::vector<Move> getPossibleMoves(const Board& board) const = 0;`: 각 파생 클래스가 자신만의 고유한 이동 규칙을 구현하도록 강제합니다.
    -   `virtual wchar_t getUnicodeChar() const = 0;`: 터미널에 기물을 시각적으로 표현하기 위한 유니코드 문자를 반환합니다.

#### 구체 파생 클래스
`Piece` 클래스를 상속받아 `Pawn`, `Rook`, `Knight`, `Bishop`, `Queen`, `King` 클래스를 구현합니다. 각 클래스는 `getPossibleMoves` 함수를 재정의하여 고유한 이동 규칙을 구현합니다.

### 1.2 `Board`와 `Square` 클래스: 상태와 소유권 관리

`Board` 클래스는 게임 상태의 중심 허브 역할을 하며, `Square`는 보드의 각 칸을 나타냅니다.

#### `Square` 클래스
-   **속성**: `int file` (열, 0-7)과 `int rank` (행, 0-7)로 좌표를 표현합니다.
-   `Square` 클래스는 `Piece` 객체를 직접 소유하지 않으며, 기물의 소유권은 `Board` 클래스가 관리합니다.

#### `Board` 클래스
-   **보드 표현**: `std::array<std::array<std::unique_ptr<Piece>, 8>, 8>`를 사용하여 8x8 보드를 표현하고, `std::unique_ptr`를 통해 기물의 배타적 소유권을 명확히 합니다.
-   **게임 상태 추적**:
    -   `Color currentTurn`: 현재 턴인 플레이어.
    -   캐슬링 권한.
    -   앙파상 목표 칸.
    -   50수 규칙 카운터.

`Board` 클래스는 게임의 모든 규칙을 검증할 수 있는 '진실의 원천(source of truth)'으로 설계하는 것이 중요합니다.

### 1.3 `Move` 클래스: 행동의 캡슐화

`Move` 클래스는 하나의 완전한 수를 표현하는 간단한 데이터 객체(Plain Old Data, POD)로 설계됩니다.

-   **핵심 속성**: `Square startSquare`, `Square endSquare`.
-   **특수 이동 플래그**:
    -   `bool isCapture = false;`
    -   `bool isCastle = false;`
    -   `std::optional<Piece::Type> promotionPiece;`

---

## 제 2부: 게임 로직과 규칙 구현

핵심 클래스 설계가 완료되면, 추상적인 체스 규칙들을 구체적인 C++ 알고리즘으로 변환해야 합니다.

### 2.1 수 생성 및 유효성 검사

체스 엔진의 핵심 기능은 가능한 모든 수를 생성하는 것입니다. 초기 2인용 게임에서는 **유효 수(Legal Move) 생성 방식**을 채택하는 것이 직관적입니다. `Piece`의 `getPossibleMoves` 메서드는 의사-유효 수를 생성하고, 상위의 `Game` 클래스에서 이 수들 중 자신의 킹을 체크 상태로 만드는 수를 필터링합니다.

### 2.2 특수 이동 규칙 심층 분석

-   **캐슬링 (Castling)**: 킹과 룩의 이동 여부, 사이 칸의 비어있음, 킹의 안전 경로 확보 등 여러 조건을 `King::getPossibleMoves` 내에서 검사합니다.
-   **앙파상 (En Passant)**: `Board`에 저장된 `enPassantTargetSquare` 속성을 확인하여 `Pawn::getPossibleMoves` 내에서 구현합니다.
-   **폰 프로모션 (Pawn Promotion)**: 폰이 마지막 랭크에 도달했을 때, `Pawn::getPossibleMoves`는 승급 가능한 모든 기물에 대한 `Move` 객체를 생성합니다.

### 2.3 `Game` 클래스: 게임의 지휘자

`Game` 클래스는 게임의 전체적인 흐름을 제어합니다.

-   **책임**:
    -   `Board` 객체 소유.
    -   수 기록(`std::vector<Move>`) 유지.
    -   `makeMove(const Move& move)`: 수의 유효성을 최종 확인하고 보드 상태를 업데이트합니다.
    -   `getLegalMoves(Square square)`: 특정 기물이 둘 수 있는 모든 유효 수를 반환합니다.

### 2.4 게임 종료 판정

모든 수가 두어진 후, `Game` 클래스는 게임 종료 여부를 판별합니다.

-   **체크메이트 (Checkmate)**: 현재 턴인 플레이어의 킹이 체크 상태이고 유효한 수가 없을 때.
-   **스테일메이트 (Stalemate)**: 현재 턴인 플레이어의 킹이 체크 상태가 아니지만 유효한 수가 없을 때.
-   **기타 무승부 조건**: 50수 규칙, 3회 동형 반복, 기물 부족.

---

## 제 3부: 터미널 인터페이스와 사용자 상호작용

C++로 구현된 게임 로직과 사용자를 연결하는 터미널 인터페이스를 설계합니다.

### 3.1 보드 렌더링

현대적인 터미널 환경을 위해 **유니코드(Unicode) 표현**을 기본으로 채택하여 미려한 그래픽을 제공합니다. (예: ♔, ♞)

| 기물 (백) | 심볼 | 유니코드 | 기물 (흑) | 심볼 | 유니코드 |
| :--- | :---: | :---: | :--- | :---: | :---: |
| 킹 (King) | ♔ | U+2654 | 킹 (King) | ♚ | U+265A |
| 퀸 (Queen) | ♕ | U+2655 | 퀸 (Queen) | ♛ | U+265B |
| 룩 (Rook) | ♖ | U+2656 | 룩 (Rook) | ♜ | U+265C |
| 비숍 (Bishop) | ♗ | U+2657 | 비숍 (Bishop) | ♝ | U+265D |
| 나이트(Knight) | ♘ | U+2658 | 나이트(Knight) | ♞ | U+265E |
| 폰 (Pawn) | ♙ | U+2659 | 폰 (Pawn) | ♟ | U+265F |

### 3.2 메인 게임 루프

`main.cpp`에 구현된 메인 루프가 게임의 실행 흐름을 제어하며, 게임이 종료될 때까지 다음 과정을 반복합니다.
1.  화면 정리 및 보드 표시
2.  사용자 입력 요청
3.  입력 파싱 및 유효성 검사
4.  `game.makeMove(move)` 호출
5.  게임 종료 조건 확인

### 3.3 표준 대수 표기법(SAN) 파싱

"e4", "Nf3", "Qxd7+", "O-O"와 같은 SAN 입력을 해석하는 파서를 설계합니다. 파서는 `Game` 객체와 상호작용하여 "이 설명에 맞는 유효한 수가 정확히 하나인가?"를 질의함으로써 모호성을 해결해야 합니다.

---

## 제 4부: 체스 엔진으로의 경로 - 확장성 설계

단순한 2인용 게임을 넘어, 고성능 AI를 탑재할 수 있는 프레임워크로 전환하기 위한 설계 변경과 핵심 알고리즘을 소개합니다.

### 4.1 고성능 보드 표현: 비트보드(Bitboards)

체스 엔진의 심각한 성능 병목을 해결하기 위해 8x8 배열 대신 **비트보드**를 도입합니다. 비트보드는 64비트 정수(`uint64_t`)를 사용하여 각 비트가 체스보드의 한 칸에 대응되는 자료구조로, 복잡한 체스 연산을 단 몇 개의 CPU 명령어로 처리할 수 있게 해줍니다.

| 특징 | 8x8 배열 (Mailbox) | 비트보드 (Bitboard) |
| :--- | :--- | :--- |
| "e4에 무슨 기물이 있는가?" | **매우 빠름** | 느림 |
| "모든 룩의 이동 경로 생성" | 느림 | **매우 빠름** |
| 구현 난이도 | 쉬움 | 어려움 |
| 엔진 성능 | 낮음 | **높음** |

### 4.2 비트보드를 이용한 고급 수 생성

-   **도약 기물 (나이트, 킹)**: 공격 테이블(lookup table)을 사용하여 한 번의 조회로 모든 공격 위치를 얻습니다.
-   **슬라이딩 기물 (룩, 비숍, 퀸)**: **매직 비트보드(Magic Bitboards)** 기법을 사용하여 특정 방해물 패턴에 대한 모든 공격 경로를 미리 계산된 테이블에서 한 번의 해시 조회로 가져옵니다.

### 4.3 AI 아키텍처: 탐색과 평가

-   **미니맥스 알고리즘과 알파-베타 가지치기**: 게임 트리를 탐색하여 최선의 수를 찾는 알고리즘입니다. 알파-베타 가지치기는 불필요한 탐색을 중단하여 탐색 노드의 수를 극적으로 줄이는 최적화 기법입니다.
-   **휴리스틱 평가 함수**: 탐색이 최대 깊이에 도달했을 때 국면의 유불리를 수치로 평가하는 함수입니다.
    -   **기물 가치 (Material Balance)**: 각 기물에 점수를 매겨 합산합니다.
    -   **기물-위치 테이블 (Piece-Square Tables, PST)**: 기물의 위치에 따라 보너스/패널티 점수를 부여합니다.
    -   **점진적 평가 (Tapered Evaluation)**: 게임 단계(오프닝/미드게임/엔드게임)에 따라 평가 방식을 동적으로 조절합니다.

---

## 체스 AI 성능 개선 및 고도화 계획

현재 AI가 겪고 있는 문제점들은 탐색(Search)의 효율성과 평가(Evaluation)의 정교함이라는 두 가지 핵심 영역의 한계에서 비롯됩니다.

### 문제점 분석

1.  **성능 저하**: 게임 초반에 가능한 수가 많아 탐색 시간이 폭발적으로 증가합니다.
2.  **어색한 오프닝 및 전략 부재**: 중앙 장악, 기물 전개, 킹 안전성 등 핵심 전략 개념이 없습니다.
3.  **의미 없는 수 반복**: 평가 함수가 수의 전략적 가치를 구분하지 못해 무의미한 움직임을 반복합니다.

### 해결 방안 및 종합 로드맵

#### 1단계: 즉각적인 성능 및 안정성 향상
-   **오프닝 북 (Opening Book) 도입**: 전문가들이 검증한 최적의 오프닝 라인을 데이터베이스에서 찾아 즉시 응수하여 초반 계산 시간을 0으로 단축합니다.
-   **반복적 심화 (Iterative Deepening) 탐색**: 탐색 깊이를 1부터 점진적으로 늘려가며, 이전 탐색 결과를 활용해 알파-베타 가지치기 효율을 극대화합니다.

#### 2단계: AI의 '체스 지능' 향상
-   **평가 함수 고도화 (기초)**:
    -   **기물 기동성 (Piece Mobility)**: 기물이 갈 수 있는 곳이 많을수록 보너스 점수를 부여합니다.
    -   **킹 안전성 (King Safety)**: 캐슬링에 보너스를, 킹 주변 폰 장벽이 무너졌을 때 페널티를 부과합니다.

#### 3단계: 정교함 추가
-   **평가 함수 고도화 (심화)**:
    -   **폰 구조 (Pawn Structure)**: 고립된 폰, 더블 폰에 페널티를, 통과한 폰에 큰 보너스를 부여합니다.
    -   **중앙 장악 (Center Control)**: 중앙 칸을 공격하는 기물에 보너스를 부여합니다.
-   **반복 수 페널티 (Repetition Penalty)**: 명확한 계획 없이 시간을 끄는 것을 방지하기 위해 포지션 반복을 일으키는 수에 약간의 페널티를 부과합니다.

---

## 결론 및 향후 과제

본 보고서는 객체지향 원칙에 기반한 C++ 터미널 체스 게임의 설계부터 고성능 체스 엔진으로의 확장 경로까지 포괄적인 청사진을 제시했습니다. 핵심 원칙은 **책임 분리**, **명시적 소유권 관리**, **확장성**입니다.

향후 과제는 다음과 같습니다.
1.  **매직 비트보드 구현**: 슬라이딩 기물 수 생성 속도를 극대화합니다.
2.  **평가 함수 고도화**: 킹 안전성, 폰 구조 등 더 정교한 평가 요소를 추가합니다.
3.  **UCI(Universal Chess Interface) 프로토콜 구현**: 표준 GUI와 통신하여 다른 엔진과 대국 및 분석을 용이하게 합니다.
4.  **탐색 알고리즘 개선**: Null-move pruning, 후기 이동 감소 등 고급 탐색 기법을 도입합니다.

이러한 단계를 통해 본 프로젝트는 단순한 터미널 게임을 넘어 강력하고 경쟁력 있는 체스 엔진으로 발전할 잠재력을 지니게 될 것입니다.

---

## 부록 A: 전체 소스 코드

(여기에 제 1~3부에서 설계한 2인용 터미널 체스 게임의 완전하고 주석이 달린 C++ 소스 코드를 첨부합니다.)